{
  "version": 2,
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm install && CI=false npm run build && cd .. && rm -rf build && cp -R frontend/build build; else npm install && CI=false npm run build; fi",
  "outputDirectory": "build",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
